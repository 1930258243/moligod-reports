name: watch357
on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  watch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run watch 357
        run: python watch_357.py
        env:
          SCT_KEY: ${{ secrets.SCT_KEY }}
      - name: Commit state
        run: |
          git config user.name "moligod-bot"
          git config user.email "moligod-bot@users.noreply.github.com"
          if [ -f watch357_state.json ]; then
            git add watch357_state.json
            git diff --cached --quiet || (git commit -m "watch357 state update" && git push)
          fi
